{
  "name": "lokalnie-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply lokalnie-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply lokalnie-db --remote",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.115.0"
  }
}
